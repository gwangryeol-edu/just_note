# just_note 기능 스펙

로그인 없이 닉네임과 비밀번호로 글을 쓰고, 수정·삭제 시 비밀번호로 본인 확인을 하는 마크다운 텍스트 게시판.

## 기술 스택

- Backend: Spring Boot 3, Spring Data JPA, MySQL
- Frontend: React (Vite)
- 반응형 없음(웹 전용), 다크모드 미지원

## 화면 구성

흰 배경, 좌/중/우 3분할 고정 레이아웃. 페이지 단위 이동이 아니라 한 화면 안에서 표시 상태가 전환되는 구조. (상세 흐름은 FIGMA_BRIEF.md 참고)

- 왼쪽: 네비게이션 (`list` / `create` / `find`)
- 가운데: list 영역 — note 전체 목록을 원형 배치로 표시, 또는 find 검색 결과로 대체
- 오른쪽: detail 영역 — 선택된 note 전체 내용, 또는 create/update 입력폼

## list (가운데 영역, 기본 상태)

- 중앙에 "just note" 텍스트, 주위를 감싸는 원형 배치로 작은 박스 10개
- 각 박스에는 note 제목의 첫 글자만 표시
- 정렬 기준은 작성일(createdAt). 새 글은 최신 자리에 추가되고, 기존 글을 수정해도 순서는 유지됨
- 10개 박스 자리는 화면에 고정. 스크롤에 따라 그 10칸에 표시되는 note가 교체되는 virtualized window 방식(커서 기반)으로 구현. 카드가 계속 추가로 그려지는 무한스크롤 아님
  - 아래로 스크롤 → 오래된 note 방향으로 창 이동
  - 위로 스크롤 → 최신 note 방향으로 창 이동
- 박스 클릭 → 오른쪽 detail에 해당 note 전체 내용 표시
- API 실패 시: "잠시 후 다시 시도해보세요" 메시지 + 새로고침 버튼

### 수정 표시

- detail에서 절대시간으로 원본 작성 시간과 수정된 시간을 모두 표시 (수정된 경우)

## create (오른쪽 detail 영역에 입력폼으로 표시)

입력 항목: 작성자(닉네임) / 비밀번호 / 제목 / 내용(마크다운)

- 작성자: 최대 50바이트(UTF-8 기준, 한글 3바이트 계산 필요), 실시간 바이트 카운터 표시 (예: "12/50 byte"). 한글·영어·공백만 허용, 맨 앞/뒤 공백 및 전체 공백 불가
- 비밀번호: 영문(대소문자 구분) + 숫자만, 25자 이내. 저장 시 반드시 해시(bcrypt)
- 제목: 최대 50자
- 내용: 마크다운, 최대 1000자, 실시간 글자 수 카운터 표시 (예: "320/1000")
- 마크다운 편집: "작성" / "미리보기" 탭 전환 방식
- 입력칸 기본 가이드: 회색 placeholder 텍스트로 작성 규칙 안내
- 유효성 검증: 형식이 안 맞으면 입력칸 테두리가 빨간색으로 바뀌고 하단에 "형식에 맞게 다시 입력해주세요." 표시. 형식 오류 상태에서 submit 누르면 저장되지 않고 오류 필드로 스크롤 이동
- 저장 성공 시: 가운데 원형에 새 박스 추가

## find (가운데 하단 입력 → 원형 대체)

- 가운데 하단에 작성자 + 비밀번호 입력폼
- 서버는 닉네임과 비밀번호가 정확히 일치하는 글만 조회해서 반환 (실제 로그인 계정 개념이 아니라 글 단위 소유권 확인)
- 조회 성공 시 가운데 원형이 검색 결과로 대체됨. 일치하는 note만 원형 배치로 표시되고, 10개 미만이면 그만큼만 표시
- 일치하는 글이 없으면: "일치하는 note가 없습니다. 작성자 혹은 비밀번호를 다시 확인해주세요" 표시 (닉네임 존재 여부는 별도로 알려주지 않음 — 추측 공격 방지)
- 검색 결과의 박스 클릭 → detail에 내용 + update / delete 버튼 표시
- 조회 시 입력했던 비밀번호는 프론트엔드에서 세션 동안(브라우저 상태/sessionStorage) 임시 보관해서, update/delete 요청 시 자동으로 함께 전송. 사용자에게 다시 묻지 않음. 단, 백엔드는 매 update/delete 요청마다 실제로 비밀번호를 재검증함
- update: detail 자리에 create와 동일한 폼이 열리고 기존 작성 내용이 채워진 상태로 시작. 저장 성공 시 find 결과 원형으로 복귀
- delete: "『{제목}』 note를 정말 삭제하시겠습니까" 확인창을 거친 후 삭제, 원형에서 해당 박스 제거

## 네비게이션 동작

- `list` 클릭 → 현재 상태(검색결과, 작성폼 등) 초기화, 가운데는 전체 note 원형 목록으로, 오른쪽 detail은 빈 상태로 복귀

## 보안 / 기타 구현 메모

- 비밀번호는 평문 저장 금지, bcrypt 등으로 해시 저장
- 마크다운 렌더링 시 사용자가 입력한 HTML/스크립트가 실행되지 않도록 sanitize 처리 필요 (예: rehype-sanitize, DOMPurify)
- 작성자 바이트 길이 검증은 문자열 길이가 아니라 실제 UTF-8 바이트 수 기준으로 계산

## 아직 미정

- 원형에서 최신/오래된 방향(시계 방향 등)
- note가 하나도 없을 때 원형 표시 방식
- detail 기본(미선택) 상태 표시 내용
- create 저장 직후 detail 상태
