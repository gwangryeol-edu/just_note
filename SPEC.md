# just_note 기능 스펙

로그인 없이 닉네임과 비밀번호로 글을 쓰고, 수정·삭제 시 비밀번호로 본인 확인을 하는 마크다운 텍스트 게시판.

## 기술 스택

- Backend: Spring Boot 3, Spring Data JPA, MySQL
- Frontend: React (Vite), react-router
- 반응형 웹, 다크모드 미지원

## 화면 구성 / 라우팅

- `/` 또는 `/notes` : note-list
- `/notes/:id` : 상세보기 (별도 페이지, URL로 공유/새로고침 유지 가능)
- `/notes/new` : note-create
- `/my-notes` : my-note

상단 로고(텍스트 "just-note") + 네비게이션 바: note-list | note-create | my-note

## note-list

- 최신순 정렬 (작성일 = createdAt 기준). 새 글은 맨 위에 추가되고, 기존 글을 수정해도 목록상 순서는 그대로 유지됨
- 한 번에 8개씩 커서 기반 인피니티 스크롤 (offset 아님 — 마지막 항목의 id/createdAt 기준으로 다음 페이지 요청)
- 미리보기: 마크다운 문법 제거한 plain text로 일정 길이만 표시, "..."로 자름, 클릭 시 상세보기로 이동
- 스켈레톤 UI로 로딩 상태 표시
- 뒤로가기로 돌아왔을 때 스크롤 위치 유지
- 빈 상태: "Let's create the first note!" 문구 + 하단에 note-create로 이동하는 버튼
- 더 이상 불러올 글이 없을 때: 화면 하단에 안내 메시지 3초간 표시 후 페이드아웃
- API 실패 시: "잠시 후 다시 시도해보세요" 메시지 + 새로고침 버튼

### 시간 표시 규칙 (미리보기)

- 10분 이내: 1분 단위 ("3분 전")
- 10분~60분: 10분 단위
- 60분~24시간: 1시간 단위
- 24시간 이후: "n일 전"

### 수정 표시

- 미리보기: 수정됨 표시 + 수정된 시각 기준으로 시간 표시 (단, 목록 순서에는 영향 없음)
- 상세보기: 절대시간으로 원본 작성시간과 수정된 시간 모두 표시

## note-create

입력 항목: 작성자(닉네임) / 비밀번호 / 제목 / 내용(마크다운)

- 작성자: 최대 50바이트(UTF-8 기준, 한글 3바이트 계산 필요), 실시간 바이트 카운터 표시 (예: "12/50 byte"). 한글·영어·공백만 허용, 맨 앞/뒤 공백 및 전체 공백 불가
- 비밀번호: 영문(대소문자 구분) + 숫자만, 25자 이내. 저장 시 반드시 해시(bcrypt)
- 제목: 최대 50자
- 내용: 마크다운, 최대 1000자, 실시간 글자 수 카운터 표시 (예: "320/1000")
- 마크다운 편집: "작성" / "미리보기" 탭 전환 방식
- 입력칸 기본 가이드: 회색 placeholder 텍스트로 작성 규칙 안내
- 유효성 검증: 형식이 안 맞으면 입력칸 테두리가 빨간색으로 바뀌고 하단에 "형식에 맞게 다시 입력해주세요." 표시. 형식 오류 상태에서 submit 누르면 저장되지 않고 작성자/비밀번호 입력 영역으로 스크롤 이동
- 저장 성공 시: note-list로 자동 이동 + 화면 중앙에 완료 메시지 3초 표시 후 페이드아웃

## my-note

- 작성자(닉네임) + 비밀번호 입력 폼
- 서버는 닉네임과 비밀번호가 정확히 일치하는 글만 조회해서 반환 (실제 로그인 계정 개념이 아니라 글 단위 소유권 확인)
- 일치하는 글이 없으면: 화면 중앙에 "일치하는 note가 없습니다. 작성자 혹은 비밀번호를 다시 확인해주세요" 표시 (닉네임 존재 여부는 별도로 알려주지 않음 — 추측 공격 방지)
- 조회 결과는 note-list와 동일한 카드 형태로 표시하되, 각 글에 update / delete 버튼 추가
- 조회 시 입력했던 비밀번호는 프론트엔드에서 세션 동안(브라우저 상태/sessionStorage) 임시 보관해서, update/delete 요청 시 자동으로 함께 전송. 사용자에게 다시 묻지 않음. 단, 백엔드는 매 update/delete 요청마다 실제로 비밀번호를 재검증함
- update: note-create와 동일한 폼이 열리고 기존 작성 내용이 채워진 상태로 시작
- delete: "『{제목}』 note를 정말 삭제하시겠습니까" 확인창을 거친 후 삭제

## 보안 / 기타 구현 메모

- 비밀번호는 평문 저장 금지, bcrypt 등으로 해시 저장
- 마크다운 렌더링 시 사용자가 입력한 HTML/스크립트가 실행되지 않도록 sanitize 처리 필요 (예: rehype-sanitize, DOMPurify)
- 작성자 바이트 길이 검증은 문자열 길이가 아니라 실제 UTF-8 바이트 수 기준으로 계산
